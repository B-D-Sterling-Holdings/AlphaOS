# Core LLM backend is local Ollama, called over HTTP via the Python stdlib
# (see src/llm_engine/ollama_client.py) — no third-party LLM SDK required.

# Data processing
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.22.0
tabulate>=0.9.0

# PDF processing
PyMuPDF>=1.23.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
yfinance>=1.0.0

# Development
ruff>=0.1.0
pytest>=7.4.0
